{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run python manage.py runserver",
    "format:all": "uv run black .",
    "format:check": "uv run black . --check"
  }
}
