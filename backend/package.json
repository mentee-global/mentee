{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run --no-project python manage.py runserver",
    "start": "uv run --no-project python manage.py runserver",
    "format:all": "uv run --no-project black .",
    "format:check": "uv run --no-project black . --check"
  }
}
