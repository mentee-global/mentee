import React from "react";
import { Modal } from "antd";

function AdminAppointmentModal({ data, visible }) {
  return (
    <div>
      <Modal visible={visible}></Modal>
    </div>
  );
}

export default AdminAppointmentModal;
