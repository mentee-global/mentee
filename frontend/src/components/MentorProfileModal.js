import React from "react";
import { Button } from "antd";

function MentorProfileModal() {
    return (
        <Button
            type="default"
            shape="round"
            onClick={() => console.log('GOO')}
        >Edit Profile</Button>
    )
}

export default MentorProfileModal;
